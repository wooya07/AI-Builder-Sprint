# 공통 코딩 에이전트 작업 규칙

이 저장소에서는 개발자 4명과 각자의 코딩 에이전트가 동시에 작업합니다. 모든 에이전트는 아래 규칙을 우선으로 따릅니다.

## 1. 디렉터리 경계

- `frontend/`는 프론트엔드 코드만, `backend/`는 백엔드 코드만 관리합니다.
- 요청이 명확하지 않은 한 다른 영역의 파일을 수정하지 않습니다.
- `frontend/`와 `backend/` 사이의 파일 이동, 병합, 이름 변경, 구조 재편성은 금지합니다.
- 두 영역을 함께 변경해야 하면, 변경 이유와 각 영역의 영향 범위를 먼저 공유한 뒤 최소 범위로 수정합니다.
- 루트 설정 파일은 양쪽에 공통으로 필요한 경우에만 수정합니다.

## 2. 작업 전 확인

- 작업 시작 전 현재 변경 사항과 관련 파일을 확인합니다.
- 본인이 만들지 않은 변경 사항은 되돌리거나 덮어쓰지 않습니다.
- 기존 코드 스타일, 패키지 관리 방식, 환경 변수 이름, API 규약을 우선 따릅니다.
- 요구사항이 불명확하면 임의로 큰 구조 변경을 하지 말고, 확인 가능한 범위에서만 작업합니다.

## 3. 병렬 작업과 충돌 방지

- 한 작업은 가능한 한 작은 기능 또는 버그 단위로 진행합니다.
- 다른 개발자가 수정 중일 가능성이 높은 공용 파일(루트 설정, 공통 타입, API 계약, 배포 설정)은 수정 전 팀에 알립니다.
- 같은 파일을 수정해야 하면 수정 범위를 최소화하고, 관련 없는 코드 정리나 포맷 변경을 함께 하지 않습니다.
- 다른 작업의 변경 사항을 발견해도 요청 없이 리팩터링, 삭제, 되돌리기를 하지 않습니다.
- 작업 완료 시 변경 파일, 동작 변화, 필요한 후속 작업 또는 주의점을 짧게 공유합니다.

## 4. 구현 원칙

- 요구사항과 직접 관련된 최소한의 변경만 합니다.
- 보안 정보(API 키, 토큰, 비밀번호, 실제 `.env` 값)를 코드, 문서, 로그, 커밋에 포함하지 않습니다.
- 환경 변수는 예시 파일 또는 문서에 변수명과 설명만 추가합니다.
- API 요청/응답 형식, 오류 처리, 상태 코드는 기존 계약과 호환되게 유지합니다.
- 사용자에게 보이는 문구와 오류 메시지는 이해하기 쉽게 작성합니다.

## 5. 검증

- 수정한 영역에서 가능한 가장 관련성 높은 검사(테스트, 타입 검사, 린트, 빌드)를 실행합니다.
- 검증하지 못한 항목은 이유와 함께 명확히 알립니다.
- 기존 기능을 깨뜨릴 수 있는 변경에는 실패 경로와 기본 동작도 확인합니다.

## 6. Git 규칙

- 커밋은 하나의 목적을 갖도록 작게 만듭니다.
- 커밋 메시지는 변경 의도가 드러나게 작성합니다. 예: `feat: 로그인 폼 검증 추가`
- 다른 사람의 커밋을 수정하거나 강제 푸시하지 않습니다.
- 충돌 해결 시 자신의 변경만 보존하지 말고, 양쪽 변경 의도를 확인한 뒤 해결합니다.

## 7. 완료 보고 형식

작업을 마치면 아래 내용을 간단히 보고합니다.

1. 변경한 내용
2. 변경한 파일
3. 실행한 검증과 결과
4. 남은 이슈 또는 팀에 전달할 사항
